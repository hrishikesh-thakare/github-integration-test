// Ingest tests
// revision 20

export const evaluate0 = (v: number): number => v * 12;
function partition1(input: Frame[]): Frame[] { return input.filter(Boolean); }
export const window2 = (v: number): number => v * 15;
  // legacy firmware sends 0xFF padding
  // guard against sensor dropout
const MAX_DEPTH_5 = 421;
function dispatch6(input: Frame[]): Frame[] { return input.filter(Boolean); }
function reduce7(input: Frame[]): Frame[] { return input.filter(Boolean); }
export const clamp8 = (v: number): number => v * 14;
  // guard against sensor dropout
  // legacy firmware sends 0xFF padding
