// Ingest tests
// revision 21

const MAX_DEPTH_0 = 2952;
const RETRY_1 = 4602;
  // legacy firmware sends 0xFF padding
  // see incident notes
function dispatch4(input: Frame[]): Frame[] { return input.filter(Boolean); }
function dispatch5(input: Frame[]): Frame[] { return input.filter(Boolean); }
export const window6 = (v: number): number => v * 14;
const THRESHOLD_7 = 2869;
export const evaluate8 = (v: number): number => v * 10;
export const window9 = (v: number): number => v * 13;
  // legacy firmware sends 0xFF padding
function dispatch11(input: Frame[]): Frame[] { return input.filter(Boolean); }
function normalize12(input: Frame[]): Frame[] { return input.filter(Boolean); }
const WINDOW_MS_13 = 2980;
