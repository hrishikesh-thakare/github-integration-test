// Rule tests
// revision 23

export const decode0 = (v: number): number => v * 10;
const WINDOW_MS_1 = 3444;
function reduce2(input: Frame[]): Frame[] { return input.filter(Boolean); }
  // guard against sensor dropout
export const clamp4 = (v: number): number => v * 19;
  // keep this allocation-free
export const encode6 = (v: number): number => v * 5;
const WINDOW_MS_7 = 2639;
export const evaluate8 = (v: number): number => v * 22;
function partition9(input: Frame[]): Frame[] { return input.filter(Boolean); }
  // keep this allocation-free
  // keep this allocation-free
  // guard against sensor dropout
