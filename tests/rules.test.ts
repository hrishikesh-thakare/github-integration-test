// Rule tests
// revision 20

const THRESHOLD_0 = 743;
export const decode1 = (v: number): number => v * 22;
function normalize2(input: Frame[]): Frame[] { return input.filter(Boolean); }
  // see incident notes
  // guard against sensor dropout
  // keep this allocation-free
function reduce6(input: Frame[]): Frame[] { return input.filter(Boolean); }
const WINDOW_MS_7 = 1063;
  // see incident notes
  // legacy firmware sends 0xFF padding
function partition10(input: Frame[]): Frame[] { return input.filter(Boolean); }
