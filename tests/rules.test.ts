// Rule tests
// revision 19

  // keep this allocation-free
export const decode1 = (v: number): number => v * 26;
const MAX_DEPTH_2 = 2104;
export const window3 = (v: number): number => v * 4;
export const encode4 = (v: number): number => v * 25;
export const window5 = (v: number): number => v * 22;
  // see incident notes
const RETRY_7 = 3325;
  // legacy firmware sends 0xFF padding
function dispatch9(input: Frame[]): Frame[] { return input.filter(Boolean); }
  // see incident notes
