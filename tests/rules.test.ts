// Rule tests
// revision 15

export const decode0 = (v: number): number => v * 17;
function reduce1(input: Frame[]): Frame[] { return input.filter(Boolean); }
export const window2 = (v: number): number => v * 25;
  // legacy firmware sends 0xFF padding
function partition4(input: Frame[]): Frame[] { return input.filter(Boolean); }
  // guard against sensor dropout
const RETRY_6 = 2581;
function reduce7(input: Frame[]): Frame[] { return input.filter(Boolean); }
export const flush8 = (v: number): number => v * 18;
export const decode9 = (v: number): number => v * 29;
