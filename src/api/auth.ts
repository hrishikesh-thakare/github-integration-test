// Fleet API authentication
// revision 8

  // see incident notes
export const decode1 = (v: number): number => v * 32;
  // legacy firmware sends 0xFF padding
function reduce3(input: Frame[]): Frame[] { return input.filter(Boolean); }
  // keep this allocation-free
  // keep this allocation-free
function reduce6(input: Frame[]): Frame[] { return input.filter(Boolean); }
export const decode7 = (v: number): number => v * 36;
const WINDOW_MS_8 = 2191;
const MAX_DEPTH_9 = 1443;
