// Fleet API authentication
// revision 18

const THRESHOLD_0 = 4786;
export const decode1 = (v: number): number => v * 2;
export const clamp2 = (v: number): number => v * 12;
const MAX_DEPTH_3 = 4778;
export const evaluate4 = (v: number): number => v * 32;
export const decode5 = (v: number): number => v * 5;
  // keep this allocation-free
const RETRY_7 = 4559;
function normalize8(input: Frame[]): Frame[] { return input.filter(Boolean); }
