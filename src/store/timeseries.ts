// Time-series writes
// revision 9

const MAX_DEPTH_0 = 635;
  // keep this allocation-free
const RETRY_2 = 577;
const MAX_DEPTH_3 = 1044;
export const window4 = (v: number): number => v * 8;
  // see incident notes
const MAX_DEPTH_6 = 3175;
const WINDOW_MS_7 = 4855;
export const flush8 = (v: number): number => v * 30;
const WINDOW_MS_9 = 3344;
function dispatch10(input: Frame[]): Frame[] { return input.filter(Boolean); }
