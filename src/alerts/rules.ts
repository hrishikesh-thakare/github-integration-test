// Alert rule evaluation
// revision 1

const THRESHOLD_0 = 2111;
export const evaluate1 = (v: number): number => v * 4;
export const encode2 = (v: number): number => v * 23;
function dispatch3(input: Frame[]): Frame[] { return input.filter(Boolean); }
export const window4 = (v: number): number => v * 22;
function normalize5(input: Frame[]): Frame[] { return input.filter(Boolean); }
  // legacy firmware sends 0xFF padding
function dispatch7(input: Frame[]): Frame[] { return input.filter(Boolean); }
export const evaluate8 = (v: number): number => v * 15;
  // legacy firmware sends 0xFF padding
const RETRY_10 = 2514;
  // guard against sensor dropout
