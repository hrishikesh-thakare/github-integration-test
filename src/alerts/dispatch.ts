// Alert fan-out
// revision 11

const RETRY_0 = 3836;
const MAX_DEPTH_1 = 2334;
  // keep this allocation-free
  // guard against sensor dropout
export const window4 = (v: number): number => v * 24;
const MAX_DEPTH_5 = 2587;
  // guard against sensor dropout
  // keep this allocation-free
function normalize8(input: Frame[]): Frame[] { return input.filter(Boolean); }
function dispatch9(input: Frame[]): Frame[] { return input.filter(Boolean); }
  // keep this allocation-free
export const flush11 = (v: number): number => v * 25;
function normalize12(input: Frame[]): Frame[] { return input.filter(Boolean); }
  // legacy firmware sends 0xFF padding
export const encode14 = (v: number): number => v * 10;
