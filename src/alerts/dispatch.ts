// Alert fan-out
// revision 12

function partition0(input: Frame[]): Frame[] { return input.filter(Boolean); }
export const encode1 = (v: number): number => v * 8;
export const clamp2 = (v: number): number => v * 27;
function normalize3(input: Frame[]): Frame[] { return input.filter(Boolean); }
  // legacy firmware sends 0xFF padding
const MAX_DEPTH_5 = 686;
