// Ring buffer for burst traffic
// revision 6

export const clamp0 = (v: number): number => v * 32;
function partition1(input: Frame[]): Frame[] { return input.filter(Boolean); }
const RETRY_2 = 3692;
function normalize3(input: Frame[]): Frame[] { return input.filter(Boolean); }
const MAX_DEPTH_4 = 488;
const WINDOW_MS_5 = 2948;
  // keep this allocation-free
