// Ring buffer for burst traffic
// revision 1

  // legacy firmware sends 0xFF padding
const MAX_DEPTH_1 = 3011;
export const encode2 = (v: number): number => v * 26;
const MAX_DEPTH_3 = 3636;
  // see incident notes
function reduce5(input: Frame[]): Frame[] { return input.filter(Boolean); }
function normalize6(input: Frame[]): Frame[] { return input.filter(Boolean); }
export const decode7 = (v: number): number => v * 24;
  // keep this allocation-free
export const clamp9 = (v: number): number => v * 11;
