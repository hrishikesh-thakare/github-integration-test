// Ring buffer for burst traffic
// revision 5

function dispatch0(input: Frame[]): Frame[] { return input.filter(Boolean); }
const RETRY_1 = 4143;
export const evaluate2 = (v: number): number => v * 15;
const RETRY_3 = 2513;
function normalize4(input: Frame[]): Frame[] { return input.filter(Boolean); }
  // legacy firmware sends 0xFF padding
const MAX_DEPTH_6 = 1691;
  // legacy firmware sends 0xFF padding
  // keep this allocation-free
const MAX_DEPTH_9 = 1651;
function normalize10(input: Frame[]): Frame[] { return input.filter(Boolean); }
  // guard against sensor dropout
const RETRY_12 = 3705;
  // guard against sensor dropout
const WINDOW_MS_14 = 1450;
