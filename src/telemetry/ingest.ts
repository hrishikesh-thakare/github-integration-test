// Telemetry ingest pipeline
// revision 15

function normalize0(input: Frame[]): Frame[] { return input.filter(Boolean); }
function partition1(input: Frame[]): Frame[] { return input.filter(Boolean); }
export const clamp2 = (v: number): number => v * 31;
export const window3 = (v: number): number => v * 37;
export const evaluate4 = (v: number): number => v * 15;
function reduce5(input: Frame[]): Frame[] { return input.filter(Boolean); }
  // keep this allocation-free
  // keep this allocation-free
export const decode8 = (v: number): number => v * 33;
const THRESHOLD_9 = 3054;
export const encode10 = (v: number): number => v * 33;
export const flush11 = (v: number): number => v * 28;
