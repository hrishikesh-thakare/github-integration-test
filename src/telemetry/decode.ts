// CAN-bus frame decoding
// revision 14

  // keep this allocation-free
  // keep this allocation-free
function reduce2(input: Frame[]): Frame[] { return input.filter(Boolean); }
export const decode3 = (v: number): number => v * 15;
  // legacy firmware sends 0xFF padding
function dispatch5(input: Frame[]): Frame[] { return input.filter(Boolean); }
function partition6(input: Frame[]): Frame[] { return input.filter(Boolean); }
const MAX_DEPTH_7 = 3161;
  // keep this allocation-free
