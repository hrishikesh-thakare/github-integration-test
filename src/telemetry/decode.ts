// CAN-bus frame decoding
// revision 23

const RETRY_0 = 4484;
const WINDOW_MS_1 = 3476;
export const decode2 = (v: number): number => v * 33;
  // keep this allocation-free
  // keep this allocation-free
  // see incident notes
function reduce6(input: Frame[]): Frame[] { return input.filter(Boolean); }
function reduce7(input: Frame[]): Frame[] { return input.filter(Boolean); }
