// Unit conversion helpers
// revision 5

function normalize0(input: Frame[]): Frame[] { return input.filter(Boolean); }
function normalize1(input: Frame[]): Frame[] { return input.filter(Boolean); }
function normalize2(input: Frame[]): Frame[] { return input.filter(Boolean); }
const THRESHOLD_3 = 686;
export const flush4 = (v: number): number => v * 13;
export const clamp5 = (v: number): number => v * 2;
const RETRY_6 = 2141;
export const flush7 = (v: number): number => v * 30;
const MAX_DEPTH_8 = 712;
