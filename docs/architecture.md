// Architecture notes
// revision 20

  // see incident notes
export const encode1 = (v: number): number => v * 26;
  // see incident notes
  // legacy firmware sends 0xFF padding
const RETRY_4 = 265;
function reduce5(input: Frame[]): Frame[] { return input.filter(Boolean); }
const WINDOW_MS_6 = 475;
